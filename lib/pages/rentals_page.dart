import 'package:flutter/material.dart';

class RentalsPage extends StatelessWidget {
  const RentalsPage({super.key});
  @override
  Widget build(BuildContext context) =>
      Center(child: Text("rentals", style: TextStyle(color: Colors.white)));
}
